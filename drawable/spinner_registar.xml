<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white" /> <!-- Cor de fundo -->
    <corners android:radius="20dp" /> <!-- Bordas arredondadas -->
    <stroke android:width="2dp" android:color="#CCCCCC" /> <!-- Contorno -->
    <padding
        android:left="8dp"
        android:right="8dp"
        android:top="4dp"
        android:bottom="4dp" />
</shape>
